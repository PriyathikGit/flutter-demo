import "package:flutter/material.dart";

class SliderWindow extends StatelessWidget {
  const SliderWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("sliding button");
  }
}
